/**
 * Mock Users Data (Stub)
 *
 * NOTE: Users are now managed via Supabase Auth and users table.
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { Profile } from '../types';

export const mockUsers: Profile[] = [];
