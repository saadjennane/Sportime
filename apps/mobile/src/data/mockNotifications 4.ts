/**
 * Mock Notifications Data (Stub)
 *
 * NOTE: Notifications are now managed via Supabase (notifications table) and OneSignal.
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { Notification } from '../types';

export const mockNotifications: Notification[] = [];
