/**
 * Mock Games Data (Stub)
 *
 * NOTE: Games are now managed via Admin panel and stored in Supabase.
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { SportimeGame } from '../types';

export const mockGames: SportimeGame[] = [];
