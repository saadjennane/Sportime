/**
 * Mock Challenges Data (Stub)
 *
 * NOTE: Challenges are now managed via Supabase (challenges, challenge_participants tables).
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { Match } from '../types';

export const mockChallengeMatches: Match[] = [];
