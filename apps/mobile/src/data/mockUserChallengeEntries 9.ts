/**
 * Mock User Challenge Entries Data (Stub)
 *
 * NOTE: Challenge entries are now managed via Supabase (challenge_participants, daily_bets tables).
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { UserChallengeEntry } from '../types';

export const mockUserChallengeEntries: UserChallengeEntry[] = [];
