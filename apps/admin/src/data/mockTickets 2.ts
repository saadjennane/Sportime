/**
 * Mock Tickets Data (Stub)
 *
 * NOTE: Tickets are now managed via Supabase (user_tickets table).
 * This stub file exists only for backward compatibility with useMockStore.
 */

import { UserTicket } from '../types';

export const mockUserTickets: UserTicket[] = [];
