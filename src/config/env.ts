// This flag controls whether the app uses the live Supabase backend or local mock data.
// Set to `true` to connect to Supabase.
// Set to `false` for local development and testing with mock data.
export const USE_SUPABASE = false;
