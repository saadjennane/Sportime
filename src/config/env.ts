// This flag controls whether the app uses the live Supabase backend
// or local mock data for all operations.
// Set to `true` to connect to Supabase.
// Set to `false` for a mock-only environment for UX testing.
export const USE_SUPABASE = false;
