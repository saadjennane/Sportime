# -----------------------------------------------------------------------------
# Supabase Frontend Configuration
#
# These variables are required for the frontend application to connect to your
# Supabase project. They are safe to be exposed in the browser.
# -----------------------------------------------------------------------------

# VITE_SUPABASE_URL
# Description: The unique URL for your Supabase project.
# Where to find: In your Supabase project dashboard, go to "Project Settings" > "API" and find the "Project URL".
# Example: VITE_SUPABASE_URL="https://xxxxxxxxxxxxxxxxxxxx.supabase.co"
VITE_SUPABASE_URL=your_supabase_url_here

# VITE_SUPABASE_ANON_KEY
# Description: The public "anonymous" key for your Supabase project. This key allows
# frontend access to your database and services based on your Row Level Security (RLS) policies.
# Where to find: In your Supabase project dashboard, go to "Project Settings" > "API" and find the "Project API Keys" > "anon" "public" key.
# Example: VITE_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS..."
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here


# -----------------------------------------------------------------------------
# IMPORTANT NOTE ON API_FOOTBALL_KEY
#
# The API-Football key is a sensitive secret and MUST NOT be stored in the
# frontend environment. It is securely stored as an environment variable
# in your Supabase project's Edge Functions settings. The frontend makes
# requests to a Supabase Edge Function, which then securely calls the

# API-Football service on the backend.
# -----------------------------------------------------------------------------
